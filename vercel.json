{
  "version": 2,
  "builds": [
    {
      "src": "npx nx build:super-admin",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/api/src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE"]
    }
  ]
}